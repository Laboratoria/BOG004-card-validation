function validator(){
let numpair=0;
let numodd=0;
let numTcString = numbertc.toString().split("");
for (let i = 0; i< numbertcToString.length; i++){

  if(i % 2 === 0){   //verificamos si la posicion es par 
    if(numTcString[i] * 2 >= 10){
      numpair += ((numto))
    }
  }
}

}


